/** @type {import('next').NextConfig} */
const nextConfig = {
  // Cloudflare Pages kompatibilitáshoz szükséges
  // A @cloudflare/next-on-pages adapter kezeli a buildet
};

module.exports = nextConfig;
